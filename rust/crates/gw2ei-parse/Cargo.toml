[package]
name = "gw2ei-parse"
description = "EVTC/zevtc binary parsing (Rust port of GW2EIEvtcParser EvtcParser)"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
thiserror = "2.0.20"
# ZipWriter（测试构造容器）不需要额外 feature；deflate 为解压所必需。
zip = { version = "8.6.0", default-features = false, features = ["deflate"] }
